# Blockers

## Current Blockers
- One capability blocker for automation setup only: the recurring Codex Automation cannot be created from this shell and must be enabled manually in the Codex app using `docs/AUTOMATION_RESEARCH_SPEC.md`.

## Notes
- There is no longer a critical live gameplay blocker for the first Roblox vehicle baseline:
  - the truck assembles
  - accepts seating
  - and moves in the live blank-place project
- Missing content, art, and final audio are expected and are not blockers.
- Discord webhook delivery remains externally dependent on a real GitHub secret and cannot be confirmed from the local repo alone.
- `rojo` is still absent locally, so repo-sync convenience is missing.
- The next work is validation and tuning, not blocker removal:
  - full pickup / handoff / delivery proof
  - route readability
  - vehicle feel
